/**
 * Text required for tooltips on entity pages.
 */

export const access = `For library and archival materials, this section
will include links to the library's local record, with holdings information and
options to request the item for use. For museum collections, this section will
include information about whether or not the item is on public view as well as
a link to its digital record on the museum website, which will include relevant
information on how to access objects not currently on public view.`

export const reuse = `Reach out to the Collection to provide additional
guidance on re-use via standardized or local rights statements when possible.`
