/**
 * Text required for tooltips on entity pages.
 */

export const reuse = `Reach out to the Collection to provide additional
guidance on re-use via standardized or local rights statements when possible.`
