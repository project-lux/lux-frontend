import styled from 'styled-components'

const EntityEvent = styled.div`
  dt {
    float: left;
    clear: left;
    overflow-wrap: break-all;
  }

  dd {
    padding: 0 0 0.5em 0;
  }
`

export default EntityEvent
