import styled from 'styled-components'

const SnippetTitle = styled.span`
  font-size: 1.5em;
  color: #286dc0;
  letter-spacing: 0;
  text-align: left;
  font-weight: 500;
`

export default SnippetTitle
