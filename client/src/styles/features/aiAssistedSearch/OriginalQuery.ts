import styled from 'styled-components'

import theme from '../../theme'

const OriginalQuery = styled.span`
  border-color: ${theme.color.lightGray};
  border-width: 1px;
  border-style: solid;
  border-radius: ${theme.border.radius};
  background-color: ${theme.color.lightBabyBlue};
  padding: 0.25rem;
`

export default OriginalQuery
